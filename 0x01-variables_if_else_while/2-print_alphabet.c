#include <stdio.h>

/**
 * main - Alphabet
 *
 * Return: always 0 (success)
 */
int main(void)
{
	char ch;
		printf("Lower case alphabet\n");
		for (ch = 'a'; ch <= 'z'; ch++)
	{
		printf("%c", ch);
	}

	return (0);
}
